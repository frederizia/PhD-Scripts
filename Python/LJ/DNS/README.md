These scripts mostly read in data generated by the DNS code and plot them. Plotting includes:
-Contour plots
-3D plots
-Averaging over axis

check_NS type files are to check if the code is working correctly by testing if it satisfies the equations.
